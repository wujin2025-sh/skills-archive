---
name: sheet-to-md
description: 腾讯文档 Sheet 反写到 Obsidian MD。从在线表格第一个 Sheet 读取需求记录，仅处理 J 列=通过的记录，按需求名称匹配本地
  MD，将 F(涉及系统)/G(关联系统)/H(计划排期)/K(备注)/L(参会人员) 融入评审纪要和技术实现章节。反写时严格遵循评审纪要 9 项规范与句尾标点点号约束。触发词：sheet反写MD、腾讯文档反写、在线表格同步到MD、需求反写、@tdoc
  反写。
disable: false
---


# sheet-to-md — 腾讯文档 Sheet ➔ Obsidian MD 反写

## 功能

从腾讯文档在线表格（默认第一个 Sheet）读取需求记录，按需求名称匹配本地 MD 文件，将在线文档中的列数据精确反写融入到 MD 对应章节。

**关键更新**：
- **路径与工作区对齐** — 统一 MD 读取与写回路径为：`/Volumes/Macintosh HD_Data/obsidian/100_Projects/需求分析/`。
- **评审纪要精简模板** — 反写数据时，不再自动写入「涉及系统」和「关联系统」行（即「下发内容」），仅保留参会人员、备注语义融合与预计上线日期。
- **K列 (备注) 智能且严格语义融合** — 备注（K）融入 `三、技术实现` 与 `评审纪要` 的内容中。**严格以事实为依据，不得随意扩散**！若原有内容与备注有冲突，以备注为准。
- **日期前缀剥离与 LCS 匹配 (防误配)** — 忽略文件前 8 位日期前缀（`YYYYMMDD-`），通过提取核心标题做最长公共子序列 (LCS) 匹配，精准定位目标文件。
- **临时标记机制与自动清理** — 脚本在检测到备注更新时，插入 `<!-- REMARK_START -->\n备注：...\n<!-- REMARK_END -->` 临时 HTML 注释，由 AI 助手进行精准语义融合后清理。

---

## 📊 在线表格与评审纪要数据映射

| 在线文档列 | MD 目标章节与位置 | 反写 / 融入规范 |
| :--- | :--- | :--- |
| **L — 参会人员** | 评审纪要（首行） | `YYYY年MM月DD日经与**部门A**人员A和**部门B**人员B沟通评审通过` |
| **H — 计划排期** | 评审纪要末尾 | 更新 `预计上线日期：计划 YYYY年MM月`（不再新增预计完成时间） |
| **K — 备注** | 评审纪要编号列表 及 `三、技术实现` | 语义融合为 `①` `②` `③` 分点换行，不乱加未经确认的推测；融毕清理临时标记 |

> **注意**：F列(涉及系统)和G列(关联系统)不再自动写入评审纪要（即「下发内容」已删除），如需更新涉及系统请直接在 MD 文档中维护。

---

## 📝 评审纪要精简模板

```markdown
#### **评审纪要**

YYYY年MM月DD日经与**[部门A]** [人员A] 和 **[部门B]** [人员B] 沟通评审通过

1. **需求补充说明**：
   通过君弘/富易等终端查询持仓、负债等页面需展示证券全称。请业务老师后续另提终端需求对接新字段，后台可先灰度升级。
2. **改造范围：**
   涉及系统：参数中心
3. **各系统改造功能：**
   (1) 参数中心：
       ① 在混合参数表中新增「新股新债申购是否判断担保品」参数配置项。
4. **前端界面**：
   请业务老师后续另提终端需求对接新字段，后台可先灰度升级。
5. **接口交互：**
   (1) 具体接口内容由开发对齐。
6. **业务参数**：
   无
7. **灰度上线关注**：
   否
   具体以后续实际开发评估为准。
8. **联测涉及系统**：
   无
9. **业务关注点：**
   老版本客户端报文向下兼容。

预计上线日期：计划 YYYY年MM月
```

> **说明**：「涉及系统」和「关联系统」行（下发内容）已不在反写范围内，请在 MD 文档中直接维护。

---

## 🛠️ 执行流程与运行方式

已在 `scripts/sheet_to_md.py` 中封装了完整的在线数据获取与本地 MD 更新，可通过命令行调用：

```bash
# 默认反写融资融券表格 (rzrq)
# 访问：https://docs.qq.com/sheet/DVGtPSXpzWU1zYXNi?tab=BB08J2 (融资融券需求跟踪)
python3 ~/.workbuddy/skills/sheet-to-md/scripts/sheet_to_md.py rzrq --apply

# 反写股票质押表格 (gpzy)
# 访问：https://docs.qq.com/sheet/DVEtyb05taEFDc0xL?tab=BB08J2 (股票质押需求跟踪)
python3 ~/.workbuddy/skills/sheet-to-md/scripts/sheet_to_md.py gpzy --apply

# 反写其他需求表格 (qt)
# 访问：https://docs.qq.com/sheet/DVE9Gb0daTmtzUE9H?tab=BB08J2 (其他需求跟踪)
python3 ~/.workbuddy/skills/sheet-to-md/scripts/sheet_to_md.py qt --apply

# 预览模式 (仅比对并输出差异预览，不修改本地 MD)
python3 ~/.workbuddy/skills/sheet-to-md/scripts/sheet_to_md.py gpzy --dry-run
```

---

## 🎯 K 列 (备注) 语义融合与冲突解决准则

AI 助手在执行临时标志 `<!-- REMARK_START -->` ... `<!-- REMARK_END -->` 的语义融合时，必须遵循以下规则：

1. **以事实为依据，不得随意扩散**：严格以表格备注内容及文件已有上下文为准，绝不随意添加未经确认的猜测或外推。
2. **冲突解决**：若表格备注中的规则与原 MD 描述不一致，**以在线表格备注为准**，覆写原冲突内容。
3. **分点排版与标点**：合并入评审纪要第 3 项时，使用 `①` `②` `③` 结构换行分点；描述性语句末尾强制包含中文句号 `。`。
4. **彻底清理标记**：语义融合完成后，**必须**将 `<!-- REMARK_START -->` 和 `<!-- REMARK_END -->` 及其包覆的临时标记彻底删除。

---

## 📁 本地路径配置

- **MD 文件目录**：`/Volumes/Macintosh HD_Data/obsidian/100_Projects/需求分析/`
- **部门映射表**：`/Volumes/Macintosh HD_Data/WorkBuddy/会议纪要/人员部门对应关系表.csv`
